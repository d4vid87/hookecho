# U.S. analyst roadmap implementation evidence

Status is conservative: **existing** has repository evidence, **partial** has useful foundations
but misses roadmap acceptance criteria, and **missing** has no accepted implementation. Update a
roadmap checkbox only after its gate passes and link the test or capture here. The web gzip budget
is 4,222,000 bytes after the GEFS distribution UI measured 4,218,268 bytes in the hosted optimized
build (a 3,732-byte, 0.09% allowance over the measured bundle).

| Area | Status | Current evidence | Gate evidence |
|---|---|---|---|
| A1 field registry | Existing | `wxdata::field`; MRMS and derived snow bands, NEXRAD Level III grids, NOHRSC snowfall, plus existing HRRR/RAP/NBM and GFS/ECMWF fields; stable product and source IDs, workspace/favorite/recent IDs with legacy-slug reads; descriptor-backed search, native-value sampling, metric/U.S. display conversion without mutating scientific values, actual-frame provenance, separate device-sized display grids, and one renderer-owned palette/range and contour-default source consumed by GPU output and legends | `cargo test -p wxdata --lib`; workspace, descriptor/source-ID, unit conversion, native export, history, search, palette coverage/range, and contour-unit tests |
| A2 time alignment | Existing | `timecoord.rs`; class-specific and product-overridden observed/analysis/forecast/derived alignment, visible offsets and configurable tolerance warnings, provenance-backed exact-time model differences, workspace-persisted pane time linking, optional identical-source-object locking with nearest-valid-time fallback, native ABI nearest-past archive selection from the radar timeline, and scheduled radar/MRMS/ABI/GLM valid-time captures | Coordinator, exact-source and nearest-volume pane-link, settings compatibility, radar/MRMS/ABI/GLM alignment, compatibility, ABI object-time parsing, and live cross-source freshness capture pass; [release capture artifact](https://github.com/d4vid87/hookecho/actions/runs/35560027592) retained by CI |
| A3 requests/cancellation | Existing | hashed request identity, identical-work deduplication, shared-consumer ownership, one bounded retry inside the overall timeout, abortable network tasks, generation checks, and an explicit cancellation boundary between decode and oversized-grid pooling; a non-interruptible third-party decode may finish on its worker thread after cancellation, but its result and every later stage are dropped | Retry, shared-consumer, last-consumer cancellation, stale-result refusal, independent-pane ownership, timeout, and cancellation-boundary tests pass |
| A4 provenance/health | Existing | every registered field carries source identity, class, exact valid/received times, quality, units, and sampling policy through `FieldFrame`; the shared request book reports fetch state, last attempt/success/failure, valid-data age, cadence, retry time, and readable errors for field, placefile, and auxiliary-feed lanes in compact rows and the detailed inspector; request selectors are hashed before storage so credentials and private coordinates do not enter diagnostics | Field valid-time age, fresh/stale/fetching/failed/waiting classification, request-lane identity, stale-result refusal, and registry provenance tests pass |
| A5 persistent cache | Existing | one immutable-object interface uses native disk storage or browser IndexedDB with bounded memory fallback for MRMS, GOES ABI, and exact-identity model ranges while preserving receipt times; family quotas, integrity validation, visible degraded status, and pin-aware eviction/clear behavior are retained; selected ABI bands are resolved across the full saved radar timeline and pinned separately from the automatic quota; mutable MRMS listings and DWD latest objects use ETag/Last-Modified revalidation tied to the exact held object identity | Native receipt/key/byte round-trip, browser reload, integrity/LRU/range-identity/pin, validator identity/refusal, compatible pack-manifest, and native/WASM checks pass |
| B operational radar | Existing | AWS live-chunk provider adapter with immediate complete-volume archive fallback, progressive radial-block merge, six moments, VCP/cut/radial/latency/retry telemetry, native gate inspector, and bounded retention of prior azimuths with their original timestamps and visible oldest-gate age | Reordered/duplicate/missing/interrupted-block and fallback-state tests pass; incomplete scans retain explicitly aged prior radials while expired data drops; realtime chunks exactly match the same archived KDMX Level II fixture; native-value and gate-geometry sampling test passes |
| C radar analysis | Existing | derived radar products and detectors; versioned portable product definitions have a live-validating settings editor, are saved/imported/exported/synced with settings, and compile to a typed CPU AST with declared radar/environment inputs and deterministic limits; gate-local products use normal pane rendering and sampling; a selectable bounded MRMS reflectivity trail renders through the shared field path with threshold/window/reset controls, retains contributor age, and exports coordinate/time/value CSV; selectable registered-field boxes report exact native-value count/min/max/mean/standard deviation plus a 16-bin histogram and CSV export, and correlate the top two continuous fields at identical valid times using declared sampling; radar box selections pair two transmitted native moments into a bounded on-map scatterplot with correlation/regression and raw-pair CSV export, plus a fixed-location plot and CSV time series across cached native timeline volumes; shared beam-coverage logic reports half-power envelopes, terrain blockage, lowest usable tilt, and the best lower-beam neighboring radar in the map measure UI; cross-sections and the GPU 3D view optionally draw every transmitted tilt's 4/3-earth beam-center path, and native height-field probes warn when the sampled feature lies outside the selected beam envelope; bounded detector history exports source time/object, algorithm version, thresholds, reason codes, gate counts, and signature scores | DSL parser/type/resource/round-trip/scalar/column/aligned-sweep tests, trail order/window/reset/grid/export, native area-statistics/histogram/correlation, native moment-pair bounds/fit, chronological/deduplicated native time-series, detector-history schema, field-ID/palette coverage, beam-path/uniform packing, height-warning, and beam envelope/blockage/comparison tests pass; a full 720×400 dual-input expression evaluates once per volume in 17.7 ms, so a second GPU evaluator is not justified |
| D satellite/GLM | Existing | GLM flashes and registry-backed flash-extent density; all 16 native ABI CMIP channels plus declarative C02/C03/C01 true-color rendering across selectable CONUS, Mesoscale 1, Mesoscale 2, and full-disk scenes; catalog channels generate search rows, source dispatch, palettes, timeline following, and offline-pack bands from metadata; fixed-grid navigation, quality masking, native sampling, unequal-channel-grid reprojection, automatic reload cache, timeline-complete pinned web packs, and scheduled cross-source valid-time capture | Real GOES-19 C13 mesoscale fixture passes value, DQF, timestamp, forward/inverse projection, native sample, and display-frame checks; all-channel/scene registry, RGB quality/time/grid, direct-RGBA upload, ABI object-time, cache LRU/pinning, pack-manifest, and [live source freshness](https://github.com/d4vid87/hookecho/actions/runs/35560027592) checks pass |
| E MRMS catalog | Existing | 82 registry-backed operational fields in `mrms.rs`, including a 20-level 0.5–7.5 km reflectivity stack, composite/isothermal/hybrid-scan reflectivity, sampling heights, echo tops, VIL, AzShear/rotation tracks, hail, QPE, FLASH, bright-band, and freezing-level products; catalog entries generate layer/search rows, acquisition, draw order, and palette selection without product-specific UI or renderer branches; the native hover probe orders enabled constant-altitude fields into a timestamped vertical profile; public freshness is checked by scheduled CI and sentinel values are normalized before sampling | Descriptor/window/draw-order/vertical-level coverage, stable-ID uniqueness, missing-value, palette coverage, native-unit, and official feed-contract tests pass; optional future products use the same metadata and acquisition mapping |
| F model workstation | Existing | shared HRRR/RAP/RRFS/NAM/NBM/GFS/GEFS/ECMWF source and schedule definitions, range-index decoders, registered fields, global fields, comparisons, and advanced sounding/hodograph profiles; GEFS ensemble mean, spread, control, and all 30 perturbed members are selectable, individual frames report one available member, and bounded common-cycle loaders provide a five-step 24-hour point plume plus all-member map postage stamps with a shared scale; point statistics use min/P10/median/mean/P90/max and threshold probabilities from only finite available members; RRFS v1 parallel is selectable for environment fields; REFS 10/20/30/40/50 dBZ neighborhood probabilities are selectable by forecast hour and preserve available-member provenance | Live RRFS CAPE and all five 14-member REFS probability range reads each cover 1,088,905 finite cells; live GEFS plume and postage-stamp loads returned all 31 members from one cycle; source-definition/member-bound, plume-bound, display-grid bound, missing-member statistic, compatibility, and scheduled feed-contract tests pass |
| G analysis/verification | Partial | observations, soundings, warning verification, cross-platform JSON forecast scores, METAR matching, and selectable range-cached NOAA RTMA or delayed URMA layers for 2 m temperature/dewpoint, surface pressure, and 10 m U wind; the shared field-verification export reports time-matched METAR bias, MAE, and RMSE for those analysis grids; URMA's missing sidecar index is handled with bounded 512-byte GRIB-header range reads followed by one exact message range | Grid/station metric and unit-conversion tests, GRIB-header bounds, source provenance, and official RTMA/URMA live range/decode tests pass; pending broader objective-analysis presentation and derived diagnostics |
| H workspaces | Existing | compatible workspaces, analyst presets, 1/2/4 pane layouts, camera/time link groups, and a geographic cursor with native radar or field values projected across linked panes; layouts cap at four panes and national textures respect platform GPU limits | Workspace compatibility, pane-ceiling, Mercator round-trip, field sampling, and native radar sampling tests pass |
| I GIS | Existing | cross-platform GeoJSON, KML/KMZ, and zipped Shapefile import normalizes points, lines, polygons, and holes into the shared overlay renderer and persists with settings; archive size/count/feature limits bound input, and Shapefiles require a matching WGS84 `.prj` | GeoJSON geometry/CRS, KML geometry, KMZ archive, zipped Shapefile, unsupported/missing CRS, native Clippy, and WASM checks pass |
| J 3D | Existing | GPU volume raymarch, cross sections, CAPPI slices, six selectable radar moments with per-moment palettes/units, quality budgets, threshold transfer, three-axis clipping, and a first-threshold-crossing surface mode; camera, clip, transfer, and surface changes update uniforms without rebuilding the volume | 3D state/threshold/quality/surface-uniform tests, native Clippy, WASM checks, and the GPU golden workflow cover the gate |
| K verification/cases | Partial | warning verification; deterministic forecast verification against compatible analysis grids or time-matched METAR observations; archive detector replay with live-equivalent cooldown and one-to-one space/time scoring against official storm reports; versioned portable case manifests with exact radar objects, annotations, bookmarks, and cross-platform import/export; readable Markdown case reports embed the complete reproducible manifest | Grid/station metrics, detector POD/FAR/CSI matching and cooldown, and manifest round-trip/report tests pass; pending optional cached case files |
| L route/chase | Partial | GPS, sharing, archive replay, user-configured OSRM route requests, alternate-route display, distance/ETA, active-warning intersection distance, 1 km native-value profiles for the visible registered field (including MRMS/model fields) or native single-radar sweep, forecast-field exposure arrival windows, and selected-storm closest approach/relative bearing/route-vs-storm ETA with a crossing marker; offline packs retain basemap/vector/satellite/terrain plus exact radar, GOES, selected MRMS source objects, and imported placefile/GIS source copies | Route request/GeoJSON validation, bounded geometry, polygon-hole, profile-spacing, provider-ETA arrival-window, pack-manifest compatibility/pinning/overlay-copy, native radar sampling, and synchronized storm-crossing tests, native Clippy, settings compatibility, and WASM checks pass; pending offline routing graphs |
| M output/API | Partial | broadcast/OBS presets for 1080p, 1440p, 4K, and portrait with safe margins, optional source clock, warning crawl, and branding; fixed `WIDTHxHEIGHT` offscreen rendering and atomic repeating snapshots with optional transparent PNG background; GIF/MP4 with fixed or exact source-time intervals and JSON provenance sidecars; timestamp-exact archive loop selection; streaming native-value field CSV with coordinates/units/timestamps/provenance; and a token-protected versioned local API for radar/product discovery, frame provenance, native point probes, cells, health, stills, and loops while preserving legacy routes | Exact archive-time selection, rectangular viewport/capture, interval encoding, transparent-clear behavior, sidecar schema, scientific CSV, local API route/catalog tests, native Clippy, and WASM checks pass; pending named-workspace output and feasibility-gated CF-Radial/GeoTIFF formats |
| N diagnostics | Partial | source health and request status | Continuous |
| O performance | Partial | profiling, smoke tests, bundle budget | Continuous |
| P extensions | Partial | bounded native command plugins with a versioned manifest, declared capabilities/products/config schema, and environment-based IPC that works with external Python | Manifest compatibility and refusal tests pass; pending scalar-grid, annotation, and table outputs |
| Q accessibility | Partial | keyboard/touch foundations | Continuous |
| R research | Partial | radar-derived storm cells retain their bounded observation histories and export timestamped centroid/motion JSON or CSV from the existing local-track controls | Export coverage passes; multi-source object enrichment, analyst correction, confidence, reflectivity fusion, and separately validated dual-Doppler retrieval remain pending |

## Baseline

Baseline commit: `9a0847c6521e9d9354c42cde09d291f4f2b67327`.

| Measure | Baseline | Method |
|---|---:|---|
| Main web WASM | 10,835,167 bytes raw / 3,999,279 gzip | `scripts/web/build.sh`, generated artifact |
| Lite web WASM | 149,346 bytes raw / 72,109 gzip | `scripts/web/build.sh`, generated artifact |
| Linux release executable | 39,041,288 bytes | `cargo build --release -p hookecho` |

The web gate is currently 4,222,000 gzip bytes. The cross-platform GIS import measured 4,126,418
bytes after replacing general Shapefile, ZIP, and XML libraries with bounded readers for the
required geometry and archive formats. Portable cases measured 4,134,935 bytes; the route adapter,
map display, warning intersection, and native-value profile measured 4,146,896 bytes. The ceiling
keeps a measured sub-0.1% regression margin without hiding the cost of later roadmap work.

Startup, resident memory, and interactive frame timing are hardware-dependent. Capture them with
`scripts/perf/capture.sh` and the in-app performance panel on the release-test hardware before the
foundation promotion; do not turn one developer-machine run into a universal threshold.

The initial route adapter follows the official OSRM v5.24 Route service contract (`route/v1`,
GeoJSON geometry, full overview, and optional alternatives), verified on 2026-09-20:
<https://project-osrm.org/docs/v5.24.0/api/>.
