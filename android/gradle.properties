org.gradle.jvmargs=-Xmx2048m
# Kotlin sources (activity, alert service/worker, widget) pull androidx — appcompat,
# games-activity and work-runtime all require it.
android.useAndroidX=true
android.nonTransitiveRClass=true
