<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately plain: a widget can only use RemoteViews, and the whole point of this one is
     to be readable at a glance on the home screen. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_alerts_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#CC101418"
    android:padding="12dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Hook Echo-WX"
        android:textColor="#8FB4D9"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/widget_alerts_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:textColor="#FFFFFF"
        android:textSize="14sp" />
</LinearLayout>
