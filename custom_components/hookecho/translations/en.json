{
  "config": {
    "step": {
      "user": {
        "title": "Hook Echo-WX",
        "description": "Point this at a machine running `hookecho --serve`.",
        "data": {
          "host": "Host",
          "port": "Port"
        }
      }
    },
    "error": {
      "cannot_connect": "Nothing answered at that address. Is `hookecho --serve` running, and is it bound somewhere this Home Assistant can reach (`--bind 0.0.0.0`)?",
      "invalid_response": "Something answered, but it wasn't a Hook Echo-WX status report.",
      "no_locations": "The server is running but has no saved locations, so there would be nothing to report. Add a marker in the app first."
    },
    "abort": {
      "already_configured": "That server is already set up."
    }
  }
}
